name = "concept-stock-screener-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.development]
name = "concept-stock-screener-api"
vars = { STAGE = "development" }

[env.production]
name = "concept-stock-screener-api"
vars = { STAGE = "production" }

# Configure KV namespaces when deployment requires them.
